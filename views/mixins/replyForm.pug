mixin replyForm(comment)
    div.reply-form
        form(action=`/comment/${comment.id}/reply/create` method='post')
            div.input-form-row
                div.input-container
                    input(type='text' name='user_name' id='user_name' required=true placeholder='Your name')
                div.input-container.wide
                    input(placeholder='Write your reply here.' required=true name='body' id='body')
                button.button.primary Reply